# Production database (api.fcmud.world) — set when prod launches
# DATABASE_URL_PROD=postgresql://user:password@host:port/dbname

# Staging/dev database (api.dev.fcmud.world) — set from alpha onwards
DATABASE_URL_DEV=postgresql://user:password@host:port/dbname

# Base URL for NFT item images (Supabase storage bucket)
NFT_IMAGE_BASE_URL=https://njqdijnpujooixoehbms.supabase.co/storage/v1/object/public/FCMImages/
